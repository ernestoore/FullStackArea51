import { ReducidorPalabrasPipe } from './reducidor-palabras.pipe';

describe('ReducidorPalabrasPipe', () => {
  it('create an instance', () => {
    const pipe = new ReducidorPalabrasPipe();
    expect(pipe).toBeTruthy();
  });
});
