import { SubirImagenDirective } from './subir-imagen.directive';

describe('SubirImagenDirective', () => {
  it('should create an instance', () => {
    const directive = new SubirImagenDirective();
    expect(directive).toBeTruthy();
  });
});
